extends: split_taxonomy_age.yaml

loss:
  age_supervised_contrastive:
    enabled: true
    weight: 0.1
    temperature: 0.07
    cross_species_positives: true

sweep:
  parameters:
    seed: [42]

wandb:
  group: generalisation-split-age-supcon
  tags: [generalisation, split-taxonomy-age, age-supcon]

slurm:
  planning:
    run_name_prefix: split_age_supcon
