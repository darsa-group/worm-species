extends: _base.yaml

model:
  multitask_architecture: single_task
  target_task: species

multi_task:
  selection_metric: species_macro_f1

training:
  gradient_diagnostics: {enabled: false, interval_steps: 100}

sweep:
  parameters:
    seed: [42]

wandb:
  group: generalisation-single-task-species
  tags: [generalisation, single-task, species]

slurm:
  planning:
    run_name_prefix: single_task_species
