extends: ../../config.yaml

matched_condition_training:
  enabled: true
  include_original: true
  deduplicate_equivalent_conditions: true
  evaluate_original_model_on_all_test_conditions: true

colour_ablation:
  enabled: false

slurm:
  planning:
    experiment_type: dual_cue
    external_expansion: dual_cue
    training_profile: cue_suppression
  cpus_per_task: 16
  memory: 16384
  time_limit: "04:00:00"
  array:
    max_active: 2
  collection:
    enabled: true
    cpus_per_task: 1
    memory: 4096
    time_limit: "00:20:00"
